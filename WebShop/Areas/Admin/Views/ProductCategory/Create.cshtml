@model ModelWeb.Models.ProductCategory

<h1>Add new Category</h1>

<hr />

<div class="row">
    <div class="col-sm-4">
        <form asp-action="Create">
            <input type="hidden" asp-for="ProductId" value="@ViewBag.ProductId" />
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="CategoryId" class="control-label"></label>
                <select asp-for="CategoryId" class="form-control" asp-items="@ViewBag.Categories">
                </select>
            </div>
            <br />
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-success" />
                &nbsp; &nbsp;
                <a class="btn btn-primary" asp-action="Index">Back to Category list</a>
            </div>
        </form>
    </div>
</div>
<br />
@*<div>
    <a class="btn btn-primary" asp-action="Index" asp-route-productId="@ViewBag.ProductId">Back to product category list</a>
</div>*@



@section Scripts{
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
    }